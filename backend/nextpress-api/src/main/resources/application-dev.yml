spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/nextpress
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  session:
    store-type: none

server:
  port: 8080
